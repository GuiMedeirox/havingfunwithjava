# Datasource do Grafana (issue #24): Prometheus auto-configurado na inicialização.
# Não precisa criar manualmente no UI — o provisionamento faz isso.
apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: true
